; watch the Bank in Lipany
; by Haretuerk

; Cops only
	? !(side player == RESISTANCE): exit

	_camera = "camera" camcreate [0,0,0]
	_camera cameraeffect ["internal", "back"]
	_camera camSetTarget [10912,3027,0]
	_camera camSetPos [10900,3058,20]
	_camera camSetFOV 0.900
	_camera camCommit 0

	~5
	_camera cameraeffect ["terminate", "back"]
	camdestroy _camera
exit; 